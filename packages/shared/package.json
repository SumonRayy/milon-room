{
  "name": "@milon-room/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Milon Room",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.2.2",
    "eslint": "^8.48.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0"
  }
} 